//import 'package:flutter/material.dart';

// ignore: camel_case_types
/*class roundedButton extends StatelessWidget {
  roundedButton({required this.title,required this.colour,required this.onP});
  final Color colour;
  final String title;
  final Function onP;
  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.symmetric(vertical: 16.0),
      child: Material(
        elevation: 5.0,
        color: colour,
        borderRadius: BorderRadius.circular(30.0),
        child: MaterialButton(
          onPressed:onP(){}
          minWidth: 200.0,
          height: 42.0,
          child: Text(
            title,
          ),
        ),
      ),
    );
  }
}
 */